Build a pixel-perfect clone of the real Airbnb listing page shown in the provided screenshot.

The goal is to reproduce the reference as precisely as possible, matching exactly how it looks and behaves. The screenshot is the **single source of truth** and should be followed without adding, removing, or creatively modifying any UI elements.

The clone must match the reference exactly in:

* **Visual design:** identical layout, spacing, typography, colors, icons, images, borders, shadows, and other visual assets.
* **Animations and motion:** replicate all visible hover effects, scroll behavior, transitions, and animations shown in the reference.
* **Interaction and accessibility:** reproduce the same interactions, keyboard navigation, focus states, clickable elements, and accessibility behavior.

Use the provided screenshot as the definitive reference for the entire implementation. The final UI should look and behave as close to the screenshot as technically possible.


make necceasrry changes according to screenshots provided and 3 screens given

photo tour will visible when user clicks on any image and preview is when user again clicks on any picture to see exanpded view so make 3 screen with its functionalities

photo tour will visible when user clicks on any image and preview is when user again clicks on any picture to see exanpded view so make 3 screen with its functionalities

photo tour will visible when user clicks on any image and preview is when user again clicks on any picture to see exanpded view so make 3 screen with its functionalitiess